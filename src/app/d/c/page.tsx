import type { Metadata } from "next";
import { DirectionC } from "./DirectionC";

export const metadata: Metadata = {
  title: "Direction C — Quiet + one signature moment",
};

export default function Page() {
  return <DirectionC />;
}
