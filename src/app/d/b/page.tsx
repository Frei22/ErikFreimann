import type { Metadata } from "next";
import { DirectionB } from "./DirectionB";

export const metadata: Metadata = {
  title: "Direction B — Light brutalist / mono",
};

export default function Page() {
  return <DirectionB />;
}
