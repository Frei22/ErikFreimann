import type { Metadata } from "next";
import { DirectionA } from "./DirectionA";

export const metadata: Metadata = {
  title: "Direction A — Dark editorial / kinetic type",
};

export default function Page() {
  return <DirectionA />;
}
